export type LoginData = {
    email: string;
    password: string;
    phoneNumber?: string;
}

export type SignUpData = {
    firstName: string;
    lastName: string;
    email: string;
    password: string;
    phoneNumber: string;
}